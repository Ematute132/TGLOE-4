export default function FinancesPage() {
  return (
    <main className="min-h-screen bg-bg px-5 py-8 md:max-w-2xl md:mx-auto">
      <h1 className="font-display text-2xl text-ink mb-2">Finances</h1>
      <p className="text-sm text-ink-dim">
        Phase 2 — connects via Plaid once set up. The savings/stocks snapshot already
        appears on the Home dashboard using placeholder data from lib/mockData.ts.
      </p>
    </main>
  );
}
