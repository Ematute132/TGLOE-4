export default function HermesPage() {
  return (
    <main className="min-h-screen bg-bg px-5 py-8 md:max-w-2xl md:mx-auto">
      <h1 className="font-display text-2xl text-ink mb-2">Hermes</h1>
      <p className="text-sm text-ink-dim">
        Phase 2 — a service on the Mac Mini holding your Canvas / Gmail / Calendar / iMessage
        connections, exposed as tools to Claude. The floating chat box on the Home PC
        dashboard and the Calendar "Add event" box both point here once it exists.
      </p>
    </main>
  );
}
